{
  "name": "nano-banana",
  "description": "AI image generation, editing, and upscaling via Gemini. Includes MCP tools and compound workflow skills.",
  "mcpServers": {
    "nano-banana": {
      "command": "bun",
      "args": ["run", "${CLAUDE_PLUGIN_ROOT}/src/index.ts"],
      "env": {
        "GEMINI_API_KEY": "${GEMINI_API_KEY}"
      }
    }
  }
}
